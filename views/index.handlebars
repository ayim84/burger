<div class="jumbotron">
  <h1 class="display-4 text-center font-weight-bold">Burgers Nom Nom Nom!</h1>
  {{!-- <p class="lead">This is a simple hero unit, a simple jumbotron-style component for calling extra attention to
    featured content or information.</p>
  <hr class="my-4">
  <p>It uses utility classes for typography and spacing to space content out within the larger container.</p>
  <a class="btn btn-primary btn-lg" href="#" role="button">Learn more</a> --}}
</div>

{{!-- <h1>Burgers Nom Nom Nom!</h1> --}}

<div class="container">
  <div class="row text-center">
    <div class="col-md-6">
      <h2 class = "mb-3">Burgers to Eat</h2>

      {{!-- <ul> --}}
        {{#each burgers}}
        {{#unless this.devoured}}

        <div class="card mx-auto" style="width: 18rem;">
          {{!-- <img class="card-img-top" src=".../100px180/?text=Image cap" alt="Card image cap"> --}}
          <div class="card-body">
            <p class="card-text">{{this.id}}. {{this.burger_name}}</p>
            <button class="devoured" data-id="{{this.id}}" data-devoured="{{true}}">Devour</button>
          </div>
        </div>


        {{!-- <p>{{this.id}}. {{this.burger_name}}</p>
        <button class="devoured" data-id="{{this.id}}" data-devoured="{{true}}">Devour</button> --}}
        {{/unless}}
        {{/each}}
      {{!-- </ul> --}}
    </div>

    <div class="col-md-6">
      <h2 class = "mb-3">Burgers Eaten</h2>

      {{!-- <ul> --}}
        {{#each burgers}}
        {{#if devoured}}

        <div class="card mx-auto" style="width: 18rem;">
          {{!-- <img class="card-img-top" src=".../100px180/?text=Image cap" alt="Card image cap"> --}}
          <div class="card-body">
            <p class="card-text">{{this.id}}. {{this.burger_name}}</p>
            {{!-- <button class="devoured" data-id="{{this.id}}" data-devoured="{{true}}">Devour</button> --}}
          </div>
        </div>


        {{!-- <p>{{this.id}}. {{this.burger_name}}</p> --}}
        {{/if}}
        {{/each}}
      {{!-- </ul> --}}
    </div>
  </div>

  <div class="col-md-12 text-center mt-5">
    <h2 class = "mb-3">Add a Burger</h2>
    <form class="createBurger">

      <div class="form-group">
        <label for="burgerName">Burger Name:</label>
        <input type="text" id="burgerName" name="name">
      </div>

      {{!-- <div class="form-group">
        <label for="sleepy">Devoured?</label><br>
        <input type="radio" name="sleepy" value="1" checked> No!<br>
        <input type="radio" name="sleepy" value="0"> Yes!
      </div> --}}

      <button type="submit">Add Burger</button>
    </form>
  </div>
</div>